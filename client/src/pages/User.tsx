import { FC } from "react";

import { Button } from "@mui/material";

import Content from "../components/Layout/Content";

const User: FC = () => {
  return (
    <Content title="User">
      <Button>user</Button>
    </Content>
  );
};

export default User;
